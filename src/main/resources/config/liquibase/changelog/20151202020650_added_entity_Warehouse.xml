<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <property name="now" value="now()" dbms="mysql,h2"/>
    <property name="now" value="current_timestamp" dbms="postgresql"/>
    <property name="now" value="sysdate" dbms="oracle"/>

    <property name="autoIncrement" value="true" dbms="mysql,h2,postgresql,oracle"/>

    <property name="floatType" value="float4" dbms="postgresql, h2"/>
    <property name="floatType" value="float" dbms="mysql, oracle"/>

    <!--
        Added the entity Warehouse.
    -->
    <changeSet id="20151202020650" author="jhipster">
        <createTable tableName="warehouse">
            <column name="id" type="bigint" autoIncrement="${autoIncrement}">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="location" type="varchar(255)">
                <constraints nullable="false" />
            </column>
        </createTable>
        
        <createTable tableName="warehouse_product">
            <column name="products_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="warehouses_id" type="bigint">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey columnNames="warehouses_id, products_id" tableName="warehouse_product"/>

        <addForeignKeyConstraint baseColumnNames="warehouses_id"
                                 baseTableName="warehouse_product"
                                 constraintName="fk_warehouse_product_product_id"
                                 referencedColumnNames="id"
                                 referencedTableName="warehouse"/>

        <addForeignKeyConstraint baseColumnNames="products_id"
                                 baseTableName="warehouse_product"
                                 constraintName="fk_product_product_warehouse_id"
                                 referencedColumnNames="id"
                                 referencedTableName="product"/>
        
    </changeSet>
</databaseChangeLog>
